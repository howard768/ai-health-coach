services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: meld
      POSTGRES_USER: meld
      POSTGRES_PASSWORD: meld_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
