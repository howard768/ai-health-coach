"""Opt-in anonymized cross-user archetype clustering.

Phase 8 deliverable. HMAC pseudonyms + Laplace DP noise + k-anonymity >= 50
+ HDBSCAN clustering. This package is imported **only** from
``backend.ml.api`` function bodies (lazy).
"""
