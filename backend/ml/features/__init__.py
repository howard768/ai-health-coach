"""Feature store + builders + catalog.

Phase 1 deliverable. This package is imported **only** from
``backend.ml.api`` function bodies (lazy). Never add a top-level import of
anything in here from outside ``backend/ml/``.
"""
