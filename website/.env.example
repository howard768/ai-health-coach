# Base URL for the FastAPI backend (no trailing slash).
# Used by WaitlistForm.astro to POST to /api/waitlist/subscribe.
#
# Local development:
# PUBLIC_API_BASE=http://localhost:8000
#
# Production (Railway):
PUBLIC_API_BASE=https://zippy-forgiveness-production-0704.up.railway.app
