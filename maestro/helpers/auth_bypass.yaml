appId: com.heymeld.app
---
- launchApp:
    arguments:
      uitesting-skip-auth: "true"

# Dismiss Apple Account Verification dialog if it appears
- tapOn:
    text: "Not Now"
    optional: true

- extendedWaitUntil:
    visible:
      id: "tab-home"
    timeout: 10000
